extends:
  - recommended

rules:
  operation-operationId-unique:
    enabled: false
    severity: off

  path-not-include-query:
    severity: warn

  no-path-trailing-slash:
    severity: warn

  operation-4xx-response:
    enabled: false
    severity: off

  spec:
    enabled: false
    severity: off

  info-license:
    severity: off
